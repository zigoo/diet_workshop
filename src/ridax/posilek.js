/* eslint-disable */
import posilki from '../helpers/posilki.js';

const initialState = {
  posilki
};


export default function posilek (state = initialState , action ) {
  switch (action.type) {
    
    default: 
      return state;
    }
}


